const Warning = ({ text }) => {
//   const validateInput = () => {

//   };

  return (
    <div id="input-warning" className="warning">
      <p>{text}</p>
    </div>
  );
};

export default Warning;
